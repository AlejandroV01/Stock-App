import React from "react";

import { IHistory } from "../../../types/api/IHistory";

interface FormattedHistory {
  x: string;
  y: string;
}

const PreviewChart = ({ data }: IHistory) => {};

export default PreviewChart;
