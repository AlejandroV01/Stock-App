"use client";

import React from "react";
import { IHistory } from "../../../types/api/IHistory";

const Candles = ({ data }: IHistory): JSX.Element => {
  return <h1>Candles chart</h1>;
};

export default Candles;
